0️⃣ 通用前缀（每次都要用）
请基于以下项目背景执行任务：

（粘贴你的 agent.md）

请严格遵守：
- 一次只完成当前任务
- 不要扩展到其他模块
- 输出要清晰、结构化

1️⃣ 架构设计模板（第一步）
（通用前缀）

当前任务：
请设计整个QA Agent系统架构：

要求：
- 主控 + 多模块结构
- 模块包括：
  - flow
  - strategy
  - testcase
  - api_test
  - report
- 每个模块独立
- 主控只负责路由

输出：
1. 模块划分
2. 数据流（输入→输出）
3. 每个模块职责
4. 模块之间如何解耦

不要写代码

2️⃣ 模块设计模板（通用）

👉 每个模块都用这个

（通用前缀）

当前任务：
设计【API测试模块】（替换成其他模块）

要求：
- 输入格式（自然语言 or JSON）
- 输出格式
- 函数列表
- 数据结构
- 边界情况处理

输出：
1. 模块职责
2. 输入输出定义
3. 函数设计
4. Prompt设计（如果用LLM）

不要写代码

3️⃣ Prompt设计模板（核心）
（通用前缀）

当前任务：
为【API测试生成】设计高质量Prompt

要求：
- 生成pytest + requests代码
- 包含正常和异常测试
- 包含断言
- 可直接运行

请输出：
1. Prompt模板
2. 示例输入
3. 示例输出
4. 如何避免生成错误代码

4️⃣ 代码实现模板
（通用前缀）

当前任务：
根据刚才的设计，实现Python代码

要求：
- 结构清晰
- 可运行
- 不要写多余功能
- 每个函数单一职责

只实现当前模块

5️⃣ 运行报错修复模板（超重要）
（通用前缀）

当前任务：
这段代码运行时报错：

（粘贴错误）

代码如下：
（粘贴代码）

请：
1. 分析错误原因
2. 给出修复方案
3. 输出修复后的完整代码


6️⃣ 代码质量审查模板（很多人忽略）
（通用前缀）

当前任务：
请审查以下代码：

（粘贴代码）

重点检查：
- 是否可运行
- 是否符合Python最佳实践
- 是否有隐藏bug
- 是否有冗余逻辑

输出改进建议 + 优化代码

7️⃣ 主控Agent设计模板
（通用前缀）

当前任务：
设计主控Agent（路由系统）

要求：
- 根据关键词判断调用模块
- 使用简单if/else
- 不使用复杂AI决策

例如：
- "API" → api_test模块
- "用例" → testcase模块

输出：
1. 路由规则
2. Python实现

8️⃣ 模块整合模板
（通用前缀）

当前任务：
整合所有模块

要求：
- 主控调用各模块
- 模块之间不直接依赖
- 支持逐步执行（不要自动流水线）

输出完整代码结构


9️⃣ Prompt优化模板（核心能力）
（通用前缀）

当前任务：
当前Prompt生成结果不好：

（粘贴输出问题）

请：
1. 分析问题原因
2. 优化Prompt
3. 给出改进版本

🔟 真实场景测试模板（最后阶段）
（通用前缀）

当前任务：
用真实场景测试系统：

输入：
“测试登录API（POST /login）”

请：
1. 模拟完整执行流程
2. 检查每一步输出
3. 指出问题
4. 给出优化建议

